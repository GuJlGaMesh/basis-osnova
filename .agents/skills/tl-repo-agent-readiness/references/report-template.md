# AI-readiness аудит репозитория: <repo-name>

## Краткий вывод

- **Итоговая оценка:** `<score>/10`
- **Рейтинг:** `<rating>`
- **Тип репозитория:** `<repo-type>`
- **Уверенность оценки:** `High | Medium | Low`
- **Применённые ограничения:** `<нет | список caps/gates>`
- **Путь к отчёту:** `[<path>](/absolute/path/to/report.md)`

Короткий абзац: сможет ли AI-агент быстро понять репозиторий, запустить нужные проверки, безопасно внести изменения и восстановиться после ошибок.

## Быстрый вердикт

- **Почему репозиторий удобен для агента:** `<1-3 коротких пункта>`
- **Где агент будет буксовать:** `<1-3 коротких пункта>`

## Покрытие evidence

- **Документация:** `<files>`
- **Манифесты и конфиги:** `<files>`
- **Исходный код:** `<files>`
- **Тесты:** `<files or none found>`
- **Автоматизация / CI:** `<files or none found>`
- **Инструкции для агентов:** `<files or none found>`
- **Чувствительные файлы, намеренно исключённые из чтения:** `<files/patterns or none>`

## Scorecard

| Категория | Оценка | Обоснование по evidence |
|----------|------:|--------------------------|
| MRC | `<score>` | `<why>` |
| NAV | `<score>` | `<why>` |
| TSC | `<score>` | `<why>` |
| TEST | `<score>` | `<why>` |
| ENV | `<score>` | `<why>` |
| MOD | `<score>` | `<why>` |
| CON | `<score>` | `<why>` |
| ERR | `<score>` | `<why>` |
| AGT | `<score>` | `<why>` |

## CI/CD evidence (информационно)

CI/CD не входит в итоговую оценку. Используй этот раздел только как операционный контекст: есть ли автоматическая обратная связь, какие проверки она запускает, и где агенту будет полезно помнить о внешних gate.

- **CI/CD файлы:** `<files or none found>`
- **Проверки:** `<build/test/lint/typecheck/deploy/none>`
- **Что это значит для агента:** `<short operational note>`

## Сильные стороны

- `<strength with evidence>`
- `<strength with evidence>`
- `<strength with evidence>`

## Главные пробелы

- `<gap with evidence>`
- `<gap with evidence>`
- `<gap with evidence>`

## Вероятные сбои агента

- `<where an agent is likely to get stuck or make unsafe changes>`
- `<where an agent will struggle to validate its work>`
- `<where hidden coupling or missing guidance will cause rework>`

## Приоритетные улучшения

1. `<highest-leverage fix>`
2. `<next fix>`
3. `<next fix>`
4. `<next fix>`

Для каждого пункта объясни, почему он повышает успешность AI-агента, и укажи часть репозитория, на которую он влияет.

## Безопасные стартовые команды для агента

Перечисли первые команды, которые агент может безопасно попробовать в этом репозитории, только если они подтверждены файлами репозитория. Если команды неясны или отсутствуют, скажи это явно.

## Evidence notes

Перечисли самые важные файлы, прочитанные для этого аудита.
